{
  "name": "yt-dlp",
  "private": true,
  "description": "Root scripts for the fork (web UI + API)",
  "scripts": {
    "install:web": "npm install && npm --prefix web/client install && python -m pip install -e ./web/server",
    "dev:client": "npm --prefix web/client run dev",
    "dev:server": "cd web/server && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev:client\" \"npm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
