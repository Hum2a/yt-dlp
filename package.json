{
  "name": "yt-dlp",
  "private": true,
  "description": "Root scripts for the fork (web UI + API)",
  "scripts": {
    "install:web": "npm install && npm --prefix web/client install && node scripts/web-venv.mjs",
    "dev:client": "npm --prefix web/client run dev",
    "dev:server": "node scripts/run-dev-server.mjs",
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev:client\" \"npm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
